<li>
  <%= gravatar_for user, size: Settings.user_gravatar %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !user.current_user?(current_user) %> |
    <%= link_to "delete", user, method: :delete,
      data: {confirm: t("you_sure")} %>
  <% end %>
</li>
